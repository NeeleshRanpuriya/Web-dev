CSS next steps:
  Alpha channel:
    a[0-1] color visible or not 
   rgba(255,255,255,1)

   Opacity:range 0 to 1

Transitions in CSS:Transition enable you to define the transition between two states of an element
  Transition Sortand:
   transition:margin-top 2s ease-in-out 0.2;

Transform;
   rotate()
        transform:rotate(-45deg) ;
   sacale()
        transform: scale(0.5);  /* scale(x,y)*/
        transition-duration: 2s;
   skew()
        transform: skew(20deg); 
   translate()
   transform:translate(50px,50px)

Box-shadow: its add shadow effects around an element's frame

  shadow:(x,y,blurradius, color)
  box-shadow:(2px 2px 10px green);

Background image:
   bacground-image=url("")
   bacground-size:contain|cover|auto;

position:
   static:
   Relative
   abolute
   fixed
   sticky
    
